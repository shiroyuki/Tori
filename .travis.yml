language: python
python:
  - "3.3"
  - "2.7"
services:
  - mongodb
  - redis
install:
  - "pip install kotoba tornado jinja2 sqlalchemy redis pymongo"
  - "pip install https://bitbucket.org/shiroyuki/ossarchive/downloads/Imagination-1.6.1a.tar.gz"
  #- "git clone https://github.com/shiroyuki/Imagination.git && cd Imagination && make install"
script: "make clean && make reset_mongodb && nosetests -c nose.cfg"
